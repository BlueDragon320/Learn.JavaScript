// Math is a built in obeject that provides a collection of properties and methods

// console.log(Math.PI);


let x = 3.132;
let y = 234;
let z;

// z = Math.round(x);          //Only increments +1 when the decimal number is greater than 0.5
// z = Math.floor(x);              //Always round DOWN
// z = Math.ceil(x);               //Always round UP
// z = Math.trunc(x);                 //Same as floor
// z = Math.pow(x, y);              //Basically to add power to a number
// z = Math.sqrt(x);                   //Squareroot
// z = Math.log(x);
// z = Math.sin(x);
// z = Math.cosin(x);
// z = Math.cos(x);
// z = Math.tan(x);
// z = Math.abs(x);                // Positive to Negative
// z = Math.sign(x);               // Negative to positive 
// z = Math.sign(x);                   // Turns answer to -1 if negative, 0 to 0, 1 to positive
// z = Math.max(x, y);              //stores largest among assigned values
// z = Math.min(x, y);              //stores smallest among assigned values

console.log(z);