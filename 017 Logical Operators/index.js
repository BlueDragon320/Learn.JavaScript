// Logical operators = used to combine or manipulate boolean values (true or false)

// AND = &&         two conditions true
// OR = ||          This or that one of the two should be true
// NOT = !          not this like it should not be this example a!=0 so this will proceed only if the input is other than 0

const temp = -100;
if (temp > 0 && temp <= 30){
    console.log("The weather is Good");
}
else {
    console.log("The weather is Bad");
}