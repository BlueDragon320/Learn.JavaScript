// = assignemtn operator 
// == comparision operator (compare if values are equal)
// === strict equality operator (compare if values & data types are equal)
// != strict inequality operator

const PI = 3.14;
if(PI !== "3.14"){
    console.log("That is Pi");
}
else {
    console.log("That is not PI");
} 